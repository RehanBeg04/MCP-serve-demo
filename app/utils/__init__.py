"""
General-purpose utilities.

Currently empty — reserved for cross-cutting helpers (e.g. retry
logic, time helpers) that don't belong to auth, logging, or MCP
specifically. Kept as an explicit package rather than omitted so
future utilities have a clear, pre-agreed home.
"""

__all__: list[str] = []